// TrayIconManager is intentionally thin — the TaskbarIcon is declared in App.xaml
// and wired directly to App.xaml.cs handlers for simplicity.
// This class is a placeholder for future tray customization (e.g. per-monitor sub-menus).

namespace BackgroundSlideShow.TrayIcon;

public class TrayIconManager
{
    // Reserved for future: dynamic per-monitor "Next >" sub-menu entries,
    // animated icon while slideshow is active, etc.
}
